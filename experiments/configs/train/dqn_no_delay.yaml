# DQN sem delay de recompensa (recompensa imediata).
include:
  - ../env/synthetic_default.yaml

env:
  reward_delay_days: 0

train:
  algorithm: dqn
  seed: 42
  epochs: 50
  n_step: 1
  batch_size: 64
  buffer_size: 20000
  lr: 0.0001
